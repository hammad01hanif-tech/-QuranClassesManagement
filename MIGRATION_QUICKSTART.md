# 🎯 الخطوات البسيطة - نقل الإعدادات إلى Firestore

## 📋 الخطوات (5 دقائق فقط!)

```
┌─────────────────────────────────────────────────────┐
│  الخطوة 1️⃣: افتح الأداة                             │
├─────────────────────────────────────────────────────┤
│  📂 افتح الملف: staff-migration-tool.html          │
│  🌐 في المتصفح (Chrome/Edge/Firefox)               │
└─────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────┐
│  الخطوة 2️⃣: معاينة البيانات                        │
├─────────────────────────────────────────────────────┤
│  👀 اضغط زر "معاينة البيانات"                     │
│  📋 راجع قائمة المعلمين الخمسة                     │
│  ✅ تأكد أن البيانات صحيحة                         │
└─────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────┐
│  الخطوة 3️⃣: تنفيذ النقل                            │
├─────────────────────────────────────────────────────┤
│  🚀 اضغط زر "بدء عملية النقل"                    │
│  ⏱️ انتظر 5-10 ثواني                              │
│  ✅ ستظهر رسالة "تمت بنجاح"                       │
└─────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────┐
│  الخطوة 4️⃣: التحقق (اختياري)                       │
├─────────────────────────────────────────────────────┤
│  🌐 اذهب إلى Firebase Console                     │
│  📊 Firestore Database                             │
│  🔍 ابحث عن staffSettings                         │
│  ✅ يجب أن ترى 5 مستندات                          │
└─────────────────────────────────────────────────────┘
```

---

## 🎬 ما الذي سيحدث؟

### قبل Migration:
```
📁 المشروع
└── data/teachers-attendance-config.js
    ├── عبدالله (ABD01)
    ├── خالد (KHL01)
    ├── محمد (MHD01)
    ├── أحمد (AHM01)
    └── يوسف (YSF01)

☁️ Firestore
└── (لا يوجد staffSettings)
```

### بعد Migration:
```
📁 المشروع
└── data/teachers-attendance-config.js
    └── (لا يزال موجود - نسخة احتياطية)

☁️ Firestore
└── staffSettings/ ✨ جديد!
    ├── ABD01
    ├── KHL01
    ├── MHD01
    ├── AHM01
    └── YSF01
```

---

## 📊 محتوى كل مستند

```javascript
staffSettings/ABD01
{
  staffId: "ABD01",
  
  workSchedule: {
    minutesAfterAsr: 35,      // بداية الدوام
    minutesAfterIsha: 30,     // نهاية الدوام
    workDays: [0,1,2,3,4]     // أيام العمل
  },
  
  penalties: {
    latePenalty: {
      amount: 5,              // 5 ريال
      intervalMinutes: 30     // كل 30 دقيقة
    },
    absencePenalty: {
      calculationMethod: "salary_divided_by_30"
    },
    earlyLeavePenalty: {
      amount: 5,
      graceMinutes: 5         // سماح 5 دقائق
    }
  },
  
  salary: {
    monthlySalary: 3000       // الراتب
  },
  
  active: true,
  createdAt: timestamp,
  updatedAt: timestamp
}
```

---

## ✅ ماذا بعد؟

بعد اكتمال Migration:

```
✅ المرحلة 1: تحديث classes (تمت)
✅ المرحلة 2: إنشاء staffSettings (تمت الآن)
⏳ المرحلة 3: تعديل attendance-calculator.js
⏳ المرحلة 4: واجهة الإدارة
```

---

## 🆘 مشكلة؟

### إذا لم تظهر الأداة:
```bash
# تأكد أنك في المجلد الصحيح
cd C:\Users\Acer\QuranClasses-webProject

# شغّل Live Server
# أو افتح الملف مباشرة في المتصفح
```

### إذا ظهر خطأ "Module not found":
```
✅ تأكد أن الملفات موجودة:
   - js/staff-migration.js
   - data/teachers-attendance-config.js
   - firebase-config.js
```

### إذا ظهر "Permission denied":
```
📱 تسجيل دخول Firebase مطلوب
🔐 تحقق من Firebase Rules
```

---

## 🎯 الهدف النهائي

```
الآن:
├── البيانات في ملف JS
└── لا يمكن تعديلها من الواجهة

بعد Migration:
├── البيانات في Firestore
├── يمكن تعديلها من الواجهة
├── يمكن للإدارة تغيير الإعدادات
└── التحديثات فورية ✨
```

---

**جاهز؟ افتح staff-migration-tool.html الآن! 🚀**
