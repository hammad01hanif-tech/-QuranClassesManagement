# تحديث النظام للتاريخ الهجري

## التغييرات المطبقة

### 1. إنشاء ملف جديد: `js/hijri-date.js`
ملف كامل لإدارة التحويل بين التاريخ الميلادي والهجري مع الحفاظ على قاعدة البيانات بالتاريخ الميلادي.

**الوظائف الرئيسية:**
- `gregorianToHijri(date)` - تحويل من ميلادي إلى هجري
- `formatHijriDate(date)` - تنسيق التاريخ الهجري بالعربية
- `gregorianToHijriDisplay(dateString)` - تحويل YYYY-MM-DD إلى عرض هجري
- `getTodayForStorage()` - الحصول على التاريخ بصيغة YYYY-MM-DD للتخزين
- `getHijriWeekAgo()` - حساب الأسبوع الماضي (7 أيام)
- `getHijriMonthAgo()` - حساب الشهر الماضي (30 يوم)

### 2. تحديث `js/main.js`
✅ تحديث دالة `updateDateTime()` لاستخدام التقويم الهجري:
```javascript
now.toLocaleDateString('ar-SA-u-ca-islamic', options)
```

### 3. تحديث `js/teacher.js`
✅ إضافة import للدوال الهجرية
✅ تحديث `loadTodayStrugglingStudents()` لعرض التاريخ الهجري
✅ تحديث `sendStrugglingToAdmin()` لحفظ التاريخ الهجري مع الميلادي
✅ تحديث `showPastReports()` لعرض التواريخ بالهجري في الجداول

### 4. تحديث `js/admin.js`
✅ إضافة import للدوال الهجرية
✅ تحديث `calculateStudentStatistics()` لاستخدام الحسابات الهجرية
✅ تحديث `loadReportsForStudent()` لعرض التواريخ بالهجري
✅ تحديث `loadStrugglingReports()` لعرض التواريخ الهجرية في التقارير

## آلية العمل

### قاعدة البيانات (Database Storage)
- **التخزين:** يبقى بالتاريخ الميلادي بصيغة `YYYY-MM-DD`
- **السبب:** الحفاظ على البيانات القديمة وتجنب اللخبطة
- **الدوال المستخدمة:** `getTodayForStorage()`

### العرض (Display)
- **جميع الواجهات:** تعرض التاريخ الهجري
- **التنسيق:** يوم الأسبوع + التاريخ الهجري بالكامل (محرم، صفر، إلخ)
- **الدوال المستخدمة:** `formatHijriDate()`, `gregorianToHijriDisplay()`

### الإحصائيات والتقارير
- **الحساب:** 7 أيام للأسبوع، 30 يوم للشهر الهجري
- **الدوال المستخدمة:** `getHijriWeekAgo()`, `getHijriMonthAgo()`

## أمثلة على التحويل

### مثال 1: عرض التاريخ في الرأس
```javascript
// قبل: الأحد 24 نوفمبر 2024
// بعد: الأحد 22 جمادى الأولى 1446
```

### مثال 2: تقرير يومي
```javascript
// Database: "2024-11-24"
// Display: "الأحد 22 جمادى الأولى 1446"
```

### مثال 3: إحصائيات أسبوعية
```javascript
// الأسبوع الماضي = آخر 7 أيام من اليوم
// يتم حسابها من التاريخ الميلادي ولكن تعرض بالهجري
```

## الملفات المعدلة

1. ✅ `js/hijri-date.js` - **جديد**
2. ✅ `js/main.js` - تحديث عرض التاريخ والوقت
3. ✅ `js/teacher.js` - تحديث جميع التواريخ المعروضة
4. ✅ `js/admin.js` - تحديث التقارير والإحصائيات

## لا حاجة لتعديل

- ❌ `firebase-config.js` - لا تغيير
- ❌ `index.html` - لا تغيير (التحديث تلقائي)
- ❌ `styles.css` - لا تغيير
- ❌ البيانات الموجودة في Firebase - تبقى كما هي

## ملاحظات مهمة

### ⚠️ التوافق مع البيانات القديمة
- جميع التواريخ المحفوظة سابقاً ستعمل بشكل صحيح
- سيتم عرضها بالهجري تلقائياً
- لا حاجة لتحديث قاعدة البيانات

### 🎯 دقة التحويل
- استخدمنا `Intl.DateTimeFormat` مع `ca-islamic` للدقة
- التحويل معتمد على متصفح المستخدم
- يدعم جميع المتصفحات الحديثة

### 📊 الإحصائيات
- الأسبوع الهجري = 7 أيام
- الشهر الهجري ≈ 30 يوم (تقريب)
- الحسابات دقيقة وتعتمد على التاريخ الميلادي الفعلي

## الاختبار

### للتأكد من عمل النظام:

1. **افتح النظام** → تحقق من التاريخ في الرأس (يجب أن يكون هجري)
2. **سجل تقييم جديد** → تحقق من التاريخ المعروض
3. **افتح التقارير السابقة** → يجب أن تظهر التواريخ بالهجري
4. **تحقق من الإحصائيات** → يجب أن تحسب بشكل صحيح
5. **افتح تقارير المتعثرين** → يجب أن تظهر التواريخ بالهجري

## الدعم الفني

إذا واجهتك أي مشكلة:
- تأكد من رفع ملف `js/hijri-date.js` على الخادم
- تأكد من تحديث الملفات الأخرى
- امسح الـ cache للمتصفح (Ctrl + F5)
- تحقق من console للمتصفح للأخطاء

---

**تاريخ التحديث:** 24 نوفمبر 2024 / 22 جمادى الأولى 1446
**الحالة:** ✅ جاهز للإنتاج
